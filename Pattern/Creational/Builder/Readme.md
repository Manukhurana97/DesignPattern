The <b>Builder Design pattern</b> is an <a href="../../../README.md#creational">creational design pattern</a> that let us create a complex object step by step, seperating the construction logic from the represention.

* Builder is used to construct complex immutable objects step by step without exposing construction logic.